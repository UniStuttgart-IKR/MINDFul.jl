function getrate(x)
    return x.rate
end

function getglobalnode_input(x)
    return x.globalnode_input
end

function getspectrumslotsrange(x)
    return x.spectrumslotsrange
end

function getopticalreach(x)
    return x.opticalreach
end

function gettransmissionmodulecompat(x)
    x.transmissionmodulecompat
end
